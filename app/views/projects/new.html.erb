<div class="container">
  <%= render 'errors' %>
</div>

<div class="container">
  <p style="color:red">Contribute to DYI</p>
  <%= bootstrap_form_for(@project) do |f| %>
      <%= render 'form', f: f %>
  <% end %>
</div>
